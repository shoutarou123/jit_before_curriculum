<div class="top">
<% provide(:title, "Top") %>
<h1>hello world</h1>
<p>人口インターン前カリキュラムのトップページです</p>

<% if !user_signed_in? %>
  <p>
    <%= link_to "ログイン", new_user_session_path %>
  </p>
<% end %>
<% if user_signed_in? %>
  <p>
    <%= link_to "ユーザー編集", edit_user_registration_path %>
    <!--button_toだとURLがusers/edit?になる-->
  </p>
<% end %>
<% if user_signed_in? %>
  <p>
    <%= link_to "letter", letter_opener_web_path %>
    <!--button_toだとURLの最後に?が付く-->
  </p>
<% end %>
</div>
